<p>Точно удалить слайд <%= @slider_item.name%>?</p>
<%= form_for(@slider_item, :method => 'delete') do |f| %>
    <%= image_tag(f.object.image_url.to_s)%>
    <h3>Текст:</h3>
    <p><%= f.object.text%></p>
    <%= f.submit("Да")%>
<% end %>
